const ScrollToTop = (className) => {
  return (
    <button
      className={`${className} h-auto w-auto rounded-full bg-yellow-500 p-3 text-white hover:bg-yellow-600`}
    >
      Top
    </button>
  );
};

export default ScrollToTop;
